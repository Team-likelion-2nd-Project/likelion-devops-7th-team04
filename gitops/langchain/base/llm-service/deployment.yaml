apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-service
  namespace: langchain
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llm-service
  template:
    metadata:
      labels:
        app: llm-service
        # infra/terraform/modules/eks/main.tf의 CPU Fargate profile이 langchain
        # 네임스페이스에서는 이 라벨(compute=fargate)이 붙은 pod만 매칭한다 — 같은
        # 네임스페이스의 ollama pod는 이 라벨이 없어 대신 GPU node group으로 간다.
        compute: fargate
    spec:
      containers:
        - name: llm-service
          image: 834922934330.dkr.ecr.us-east-1.amazonaws.com/team04-dev-llm-service:dev
          ports:
            - containerPort: 8000
          env:
            # ollama도 같은 langchain 네임스페이스라 짧은 서비스명으로 바로 붙는다.
            - name: OLLAMA_BASE_URL
              value: "http://ollama:11434"
            - name: OLLAMA_MODEL
              value: "qwen2.5:7b-instruct"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
