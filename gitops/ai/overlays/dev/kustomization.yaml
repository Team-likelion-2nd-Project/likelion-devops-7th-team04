apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
# backend-ci-cd.yml의 deploy-ai job이 ECR push 직후 이 newTag 값을
# ${GITHUB_SHA}로 갱신합니다 (yq 사용, gitops/backend/overlays/dev/kustomization.yaml과
# 동일한 패턴).
images:
  - name: 834922934330.dkr.ecr.us-east-1.amazonaws.com/team04-dev-ollama
    newTag: latest
